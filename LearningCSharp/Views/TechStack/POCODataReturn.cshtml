@model IEnumerable<LearningCSharp.Models.TechStack>

@{
    ViewData["Title"] = "POCODataReturn";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center">Retuning Data from Poco (Hardcoded)</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-dark; rounded">
    <thead>
        <tr class="bg-dark" style="color:ghostwhite">
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UsedBefore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Proficiency)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FavAtrribute)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsedBefore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proficiency)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FavAtrribute)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>

<h1 class="text-center">Returning data from the T-SQL Database</h1>